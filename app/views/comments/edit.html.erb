<%= form_for @comment, url: issue_comment_path(@issue, @comment) do |f| %>
	<%= f.label "Edit Content" %>
	<%= f.text_field :content %>
	<%= f.submit "Update" %>
<% end %>

<%= link_to "Back", issue_path(@issue) %>